ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР!
ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР!
ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР! ВКЛЮЧИ ПРОВАЙДЕР!

1. Web-сервер — NGINX (скопировать шаблон)
2. IP на if — netplan*
* — 
	network:
	version: 2 
	renderer: networkd 
	ethernets: 
		ens33: 
			dhcp4: false 
			addresses: [192.168.233.154/24] 
			gateway4: 192.168.233.2 
			nameservers:
			addresses: [1.1.1.1, 8.8.8.8]
аааа, команда netplan apply!!!!!! 
Она применит настройки вашего файла. 
И новый файл делать не надо, у вас там по-умолчанию есть файл 00-netconfig что-то там, просто поправьте значения там!!!!!
""""""""""""""""""""""""
Макар, попробуй вот так: 
	ip addr add 10.0.0.2/30  dev ens160 - поставишь айпи так
	ip route add default via 10.0.0.1 - поставишь гейтвей
потом в /etc/resolv.conf - напиши
	nameserver 8.8.8.8
проверь пинги до ya.ru
потом ставь командой - apt install network-manager -y
и ставь адрес просто через nmtui


FFFFFFFFFFFFF
все у вас ок c OPNSENSE!!! Зайдите по http://192.168.1.1 а не по https АААААААААААА, НУ ВЧЕРА ЖЕ СМОТРЕЛИ!!!
Макар - https://losst.ru/nastrojka-seti-netplan-v-ubuntu

3. Firewall OFF:
FIREWALL НА СЕРВЕРАХ ОТКЛЮЧИТЬ! FIREWALL НА СЕРВЕРАХ ОТКЛЮЧИТЬ!

	— systemctl disable --now firewalld
	— sudo ufw disable
4. Docker:
ДЛЯ ЗАПУСКА ПОСЛЕ ПЕРЕЗАГРУЗКИ ДОБАВИТЬ АРГУМЕНТ --restart

	— INSTALL: apt install docker docker.io
	— USER: usermod -aG [пользователь] docker
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
1) Проверить статус сервиса - systemctl status <имя_сервиса>
2) Перезагрузить сервис - systemctl restart <имя_сервиса>
3) Посмотреть логи - journalctl -xe

4) traceroute - команда для проверки доступности адреса, показывает вам где происходит "затык". Ключ --help для того чтобы узнать как отключить проверку по DNS
5) Для настройки маршрута - команда ip route add или через nmtui
